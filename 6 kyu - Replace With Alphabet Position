def alphabet_position(text)
  res = []
# Делаяем большие буквы маленькими, затем удаляем спецсимволы и цифры, разделяем всю фразу на отдельные буквы и собираем их в массив
# для каждого элемента массива делаем проверку
 text.downcase.delete(" !.,+-@?1234567890'").split(//).to_a.each do |n|
     if 
#эталонный массив ("a".."z").to_a проверяем на совпадение с n элементов из исходного массива - если значения равны, 
#добавляем в массив res номер символа из эталнного массива
         ("a".."z").to_a.each_with_index do |name, index| name == n ? res.push(index+1).to_s : print end
     end
 end
# выводим массив res, объединив его элементы через пробел
  return res.join(" ")
end
