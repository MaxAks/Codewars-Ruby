def mirror(data)
  data.sort[0...-1] + data.sort.reverse # sort[0...-1] - сортирует массив от минимального к максимальному, а скобки показывают, что выводится 
  # все кроме последнего элемента массива, в данном случае - это уже максимальный элемент массива, для массива [-8, 42, 18, 0, -16] получится
  # [-16, -8, 0, 18]
  # sort.reverse - переворачивает отсортированный массив.
end
