def dont_give_me_five(start_,end_)
  n = (start_..end_).to_a
  n.reject! {|i| i.to_s.include? "5" }
=begin
reject() метод - Возвращает новый массив, содержащий в себе элементы, для которых данный блок является истинным.
Порядок неотбракованных элементов сохраняется как в исходно массиве.

i.to_s - обращение к каждой переменой как к строке с содержащей еинственное значение массива, выбранное по порядку счетчика.

include? - метод возвращающий true если в массив или переменная включает в себя искомый символ или перемеенную
=end
  n = n.size # Сообщает количество элементов массива
  return n #Возвращает n
  puts n
end
puts dont_give_me_five(-12,63)
