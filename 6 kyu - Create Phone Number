# НЕ ясно почему задача имеет 6 Кату... По факту она 7 или 8. Но для тренеровки метода подошла хорошо.
def create_phone_number(numbers)
  a = numbers
return '('+ a[0..2].join + ')' + ' '+ a[3..5].join + '-'+a[6..10].join
end
#Можно решить короче через %d
